{
  "mcpServers": {
    "silica": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "--extra",
        "mcp",
        "silica",
        "mcp",
        "--extended"
      ]
    }
  }
}
